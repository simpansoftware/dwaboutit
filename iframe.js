document.open();document.write(`<!DOCTYPE html> <html lang="en"> <head> <meta charset="UTF-8"> <title>Fullscreen Iframe with User Input</title> <style> html, body { height: 100%; margin: 0; } body { display: flex; flex-direction: column; } #urlInputContainer { padding: 10px; background-color: #f0f0f0; } #urlInput { width: 80%; padding: 5px; font-size: 16px; } #loadBtn { padding: 6px 12px; font-size: 16px; cursor: pointer; } iframe { flex: 1; /* takes the remaining height */ width: 100%; border: none; } </style> </head> <body> <div id="urlInputContainer"> <input type="text" id="urlInput" placeholder="Enter URL here, e.g. https://example.com" /> <button id="loadBtn">Load</button> </div> <iframe id="myIframe" src=""></iframe> <script> const input = document.getElementById("urlInput"); const button = document.getElementById("loadBtn"); const iframe = document.getElementById("myIframe"); button.addEventListener("click", () => { let url = input.value.trim(); if (!url) return; // Add https:// if missing if (!/^https?:\/\//i.test(url)) { url = "https://" + url; } iframe.src = url; }); // Optional: press Enter to load input.addEventListener("keypress", (e) => { if (e.key === "Enter") { button.click(); } }); </script> </body> </html>`);document.close();
